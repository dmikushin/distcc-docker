# DistCC worker node container (Ubuntu 22.04)

Dockerized ditscc worker node for distributed compilation.

## Building

```
docker build -t distcc-docker .
```

## Deployment

```
docker-compose up -d
```
